function getFacebookFriends(fbLink) {
    // Assuming the fbLink is a valid Facebook page link
    const friends = ['Friend1', 'Friend2', 'Friend3']; // Hardcoded list of friends' names
    return friends;
}

module.exports = getFacebookFriends;
